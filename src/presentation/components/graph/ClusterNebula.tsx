"use client";

// ClusterNebula - renders signal cluster particles as a Three.js overlay
// This is integrated into UniverseGraph via the node coloring and grouping
// Standalone signal cluster rendering will be added in Phase 2

export function ClusterNebula() {
  // Phase 2: Three.js Points + custom shader for signal cluster effect
  return null;
}
